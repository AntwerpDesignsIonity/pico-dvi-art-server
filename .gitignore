# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Server runtime artefacts
pc_server/config.json
pc_server/ota.trigger
pc_server/art_cache/
preview/
received.png

# Downloaded MicroPython .uf2 images (tools/flash_pico.py)
.cache/

# Device credentials - MUST NEVER BE COMMITTED (the repo is public for raw OTA)
pico_firmware/device_secrets.py

# Pico OTA leftovers (only ever exist on the device, never in the repo)
*.bak
*.new
ota.pending

# Editors / OS
.vscode/
.idea/
Thumbs.db
.DS_Store
